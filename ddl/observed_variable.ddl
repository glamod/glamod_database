CREATE TABLE __INSERT_SCHEMA__.observed_variable (
  variable INT,
  parameter_group VARCHAR,
  domain VARCHAR,
  sub_domain VARCHAR,
  name VARCHAR,
  units VARCHAR,
  description VARCHAR,
  PRIMARY KEY(variable)
);
